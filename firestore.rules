rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ 임시 완전 개방 규칙 (데이터 로딩 복구용)
    // TODO: 데이터 로딩 확인 후 보안 규칙으로 복원할 것
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 